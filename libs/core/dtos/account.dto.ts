/** Interface for Account dto. */
export interface AccountDto {

  /** Email of user. */
  readonly email: string;

  /** First name of user. */
  readonly first_name: string;

  /** Last name of user. */
  readonly last_name: string;

  /** Password of user. */
  readonly password: string;

}
