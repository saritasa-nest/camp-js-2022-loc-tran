import { initAnimeTable, initPagination, initQuery } from '../script/init';
import { checkPermission } from '../script/isAuthorized';

initAnimeTable();
initPagination();
initQuery();
checkPermission();
